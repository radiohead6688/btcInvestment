#include "backtest.h"
#include <iostream>

using std::cout;
using std::endl;

//Backtest::Backtest(std::vector<DailyPrice> const& historyPrices, Strategy strategy)
        //: m_historyPrices(historyPrices), m_strategy(strategy) {

//}

void Backtest::run() {
    double quantity;
    //cout << m_strategy
    //for (auto const& i : m_historyPrices) {
        //quantity = m_strategy.getQty(i.low);

    //}
}
